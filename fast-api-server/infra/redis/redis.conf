# Focus FastAPI - Redis draft config
# Compose 내부 네트워크 사용을 전제로 한 초안입니다.

bind 0.0.0.0
port 6379
protected-mode no

# In-memory metadata cache 목적
save ""
appendonly no

databases 1
maxmemory 256mb
maxmemory-policy allkeys-lru

timeout 0
tcp-keepalive 60
